<Application x:Class="MultiDesk.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <!-- The active theme dictionary is injected at runtime by ThemeService into
             Application.Current.Resources.MergedDictionaries, so every DynamicResource
             updates with no restart. -->
    </Application.Resources>
</Application>
